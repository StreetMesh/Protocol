export const metadata = {
  title: 'Technology',
  description:
    'The open standards underneath — HTTP, DIDs, OAuth, ATProtocol — and how they fit together.',
}

# Technology

StreetMesh invents as little as it can get away with. This guide covers what it borrows — HTTP, DIDs, OAuth, ATProtocol — and the seams where the borrowed pieces are joined. {{ className: 'lead' }}

<Note>
  This guide has not been written yet. The [glossary](https://github.com/StreetMesh/Protocol/blob/main/GLOSSARY.md) and the [decisions](https://github.com/StreetMesh/Protocol/tree/main/decisions) carry what has actually been settled in the meantime.
</Note>
